import { InjectableConstants } from './examples/injectable-constants';
import { InjectableHooks } from './examples/injectable-hooks';
import { NestedComponentResolving } from './examples/nested-component-resolving';

export const stories = { InjectableConstants, InjectableHooks, NestedComponentResolving };
